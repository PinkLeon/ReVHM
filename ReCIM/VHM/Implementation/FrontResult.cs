using VHM.Interface;

namespace VHM.Implementation
{
    public class FrontResult : Result
    {

        //要從static context 或 給AOIContext,由裡面撈

        //public override List<string> Judge(List<string> measuredValues, List<TestSpectation> specfications, List<string> tolerances)
        //{
        //    List<string> results = new List<string>();
        //    //對每個項目進行,量測值,公差,標準值比對

        //    //還要進行單位轉換,將pixel轉um,mm也轉um比對
        //    //results = specfications.Select(n=>n.Item_Name).ToList();


        //    return results;
        //}
    }
}
